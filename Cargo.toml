[package]
name = "roam-proto"
version = "0.1.0"
edition = "2021"
description = "Shared Protocol Buffer definitions for ROAM Framework"

[dependencies]
prost = "0.12"
tonic = "0.11"

[build-dependencies]
tonic-build = "0.11"
protobuf-src = "1.1"
